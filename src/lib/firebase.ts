// Firebase ha sido reemplazado por Supabase.
// Este archivo existe únicamente para evitar errores de importación en código legado.
// El cliente real se encuentra en src/lib/supabase.ts

export const db = null;
export const auth = {
  currentUser: null,
};
